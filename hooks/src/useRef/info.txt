useRef - useRef is hook which return a object with property called current

uses ---
        1. useRef is used to refernce for jsx element and perform dom manipulation
          Ex --
               js -- in js we can write document.getelmentbyId("h2") 
               useRef -- const ref1 = useRef()  <h1 ref={ref1} ></h1>  

        2. we can use it for storing some data which we dont want to delete whenever our component is rerender 

